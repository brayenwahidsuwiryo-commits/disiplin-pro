(() => {
  const cfg=window.APP_CONFIG||{};if(!cfg.supabaseUrl||!cfg.supabaseAnonKey||!window.supabase)return;const sb=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
  async function school(){const{data:{user}}=await sb.auth.getUser();if(!user)throw Error('Sesi login tidak ditemukan');const{data,error}=await sb.from('profiles').select('school_id').eq('id',user.id).single();if(error)throw error;return data.school_id}
  async function getTemplate(id){const{data,error}=await sb.from('report_templates').select('*').eq('school_id',id).eq('is_default',true).maybeSingle();if(error)throw error;return data}
  async function upload(file,id){if(!file)return null;if(file.size>10*1024*1024)throw Error('File template maksimal 10 MB.');const allowed=['application/pdf','image/png','image/jpeg','image/webp'];if(!allowed.includes(file.type))throw Error('Template harus PDF, PNG, JPG, atau WEBP.');const ext=file.name.split('.').pop().toLowerCase();const path=`${id}/report-template-${Date.now()}.${ext}`;const{error}=await sb.storage.from('school-report-assets').upload(path,file,{contentType:file.type,upsert:false,cacheControl:'3600'});if(error)throw error;const{data,error:e}=await sb.storage.from('school-report-assets').createSignedUrl(path,86400);if(e)throw e;return data.signedUrl}
  async function render(){const title=($('#pageTitle')?.textContent||'').toLowerCase();if(!title.includes('template laporan'))return;const form=$('#reportTemplateForm');if(!form||form.querySelector('[data-template-reference]'))return;const id=await school();const t=await getTemplate(id);const wrap=document.createElement('section');wrap.className='card';wrap.dataset.templateReference='1';wrap.style.marginTop='16px';wrap.innerHTML=`<div class="eyebrow">ACUAN DESAIN</div><h3>Upload Template Laporan Sekolah</h3><p class="hint">Upload contoh kop/surat resmi sekolah (PDF atau gambar). File ini menjadi acuan visual saat menyusun laporan. Logo dan tanda tangan tetap dapat diunggah terpisah agar sistem bisa menempatkannya pada laporan.</p><input id="reportReferenceFile" type="file" accept="application/pdf,image/png,image/jpeg,image/webp"><div id="reportReferenceStatus" class="hint" style="margin-top:8px">${t?.template_file_url?'Template acuan sudah tersimpan.':''}</div><div id="reportReferencePreview" style="margin-top:12px"></div>`;form.parentElement.appendChild(wrap);if(t?.template_file_url){const p=wrap.querySelector('#reportReferencePreview');if(t.template_file_type?.startsWith('image/'))p.innerHTML=`<img src="${esc(t.template_file_url)}" style="max-width:100%;max-height:420px;object-fit:contain;border:1px solid var(--line);border-radius:12px">`;else p.innerHTML=`<a class="secondary" href="${esc(t.template_file_url)}" target="_blank" rel="noopener">Buka template PDF</a>`}
    wrap.querySelector('#reportReferenceFile').onchange=async e=>{try{const file=e.target.files?.[0];if(!file)return;wrap.querySelector('#reportReferenceStatus').textContent='Mengunggah template...';const url=await upload(file,id);const{error}=t?.id?await sb.from('report_templates').update({template_file_url:url,template_file_type:file.type,updated_at:new Date().toISOString()}).eq('id',t.id):await sb.from('report_templates').insert({school_id:id,name:'Template Utama',template_file_url:url,template_file_type:file.type,is_default:true});if(error)throw error;wrap.querySelector('#reportReferenceStatus').textContent='Template acuan berhasil disimpan.';if(file.type.startsWith('image/'))wrap.querySelector('#reportReferencePreview').innerHTML=`<img src="${esc(url)}" style="max-width:100%;max-height:420px;object-fit:contain;border:1px solid var(--line);border-radius:12px">`;else wrap.querySelector('#reportReferencePreview').innerHTML=`<a class="secondary" href="${esc(url)}" target="_blank" rel="noopener">Buka template PDF</a>`}catch(err){wrap.querySelector('#reportReferenceStatus').textContent='Gagal: '+err.message}};
  }
  const mo=new MutationObserver(()=>{clearTimeout(window.__rtTimer);window.__rtTimer=setTimeout(()=>render(),100)});window.addEventListener('load',()=>{render();mo.observe(document.body,{childList:true,subtree:true})});
})();
