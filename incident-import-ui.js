(() => {
  function render(){const page=document.querySelector('#page');if(!page||page.querySelector('[data-incident-import]'))return;const title=(document.querySelector('#pageTitle')?.textContent||'').toLowerCase();if(!title.includes('catatan kejadian'))return;const card=document.createElement('section');card.className='card';card.dataset.incidentImport='1';card.innerHTML='<div class="toolbar"><div><h3>Import Catatan dari Excel</h3><p class="hint">Satu file berisi 3 sheet: Pelanggaran, Prestasi, dan Sangsi. Siswa dicocokkan dengan <b>Nama + Kelas</b> dalam sekolah yang sedang login.</p></div><div class="excel-import-actions"><label class="primary" for="incidentExcelImport">📥 Pilih Excel</label><input id="incidentExcelImport" type="file" accept=".xlsx,.xls" hidden><button id="downloadIncidentTemplate" class="secondary" type="button">⬇ Template</button></div></div><div class="hint">Kolom: No., Nama, Kelas, Tanggal, Jenis Kejadian, Poin, Kategori, Kronologi, Pencatat. Kategori hanya dipakai pada sheet Pelanggaran. Master yang belum ada tidak dibuat diam-diam; import akan memberi tahu baris yang perlu dilengkapi.</div>';page.prepend(card)}
  const mo=new MutationObserver(render);window.addEventListener('load',()=>{render();mo.observe(document.body,{childList:true,subtree:true})});
})();
